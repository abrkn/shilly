module.exports = async ({ say }) => {
  //const text = await fetchDifficultyAdjustmentEstimate();
  //say(`Bitcoin Core will adjust difficulty ${text}`);
  await say(`Sorry, The !da command is unavailable until we find an accurate API`);
};
